# apps/web

React SPA (Vite + TypeScript + Tailwind). Owned by the SPA agent.

Consumes the API contract documented in `apps/api/README.md`:

- POST submission, GET state, subscribe to SSE `/events`.
- Renders 4-digit FSC codes with title, rationale, confidence.

See the SPA agent plan in `.cursor/plans/` for tasks.
